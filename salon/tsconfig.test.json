{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // テストは Node で走らせるので、Workers の型に加えて Node の型も要る。
    "types": ["@cloudflare/workers-types", "node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
